class Solution {
    public int findDuplicate(int[] nums) {
        int count[]=new int[nums.length];
        for(int i:nums)
        {
            if(count[i]==1)
            return i;
            count[i]=1;
        }
        return -1;
    }
}
